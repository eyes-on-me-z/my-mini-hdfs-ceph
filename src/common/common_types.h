#pragma once

#include <cstdint>
#include <string>

namespace mini_storage
{
    using BlockId = uint64_t;
    using FilePath = std::string;
    using DataNodeId = std::string;

    // 未完成

} // namespace mini_storage